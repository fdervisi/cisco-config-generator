---

  - name: "First Play: Automate Routing Configs via Jinja2"
    hosts: all

    vars:
      ip: []
    
    tasks: 

      - name: Setup a string variable
        set_fact:
          host_ip: "{{ ansible_host }}"
        register: ip

      - debug: var=ip